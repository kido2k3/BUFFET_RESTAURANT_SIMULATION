# BUFFET_RESTAURANT_SIMULATION

This is a simulation of buffet restaurants in reality based on simpy.
Run the following line in terminal to install all modules in this project.

```sh
pip install -r requirements.txt
```